baseURL = "https://telly.guru/"
locale = "en-US"
title = "Telly.guru"
enableRobotsTXT = true
disableKinds = ["taxonomy", "term", "RSS"]

[params]
  description = "Telly. The office oracle. Bring the hard question and leave with a sharper one."

[caches]
  [caches.images]
    dir = ":cacheDir/images"

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = false
